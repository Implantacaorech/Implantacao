{
  "name": "painel-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes ponta a ponta (Playwright) do Painel de Implantação — interface real, navegador real",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
