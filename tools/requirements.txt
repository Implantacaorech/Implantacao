# Dependências dos geradores Office + painel web
python-docx>=1.1
openpyxl>=3.1
PyYAML>=6.0
Flask>=3.0      # painel web (webapp/)
anthropic>=0.40 # modo IA: reconferência de tempo verbal + ortografia (opcional, só com chave)
SQLAlchemy>=2.0 # Hub "Projetos por Cliente" (banco; SQLite por padrão, agnóstico p/ Postgres)
psycopg2-binary>=2.9 # driver PostgreSQL (banco em Docker via PAINEL_DB_URL)
waitress>=3.0   # servidor WSGI de produção (substitui o app.run de desenvolvimento)
google-auth>=2.0          # envio pela API do Gmail (OAuth) quando o SMTP é bloqueado
google-auth-oauthlib>=1.0 # fluxo de autorização OAuth do Gmail
httpx>=0.27               # chamadas HTTPS à API do Gmail

pywin32>=306 ; sys_platform == "win32"   # Word COM p/ pré-visualização fiel (docx->PDF)
