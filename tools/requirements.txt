# Dependências dos geradores Office + ponte administrativa legada (webapp/legado_cli.py).
# O painel Flask foi desligado em 2026-07-19 e removido do repositório — Flask, waitress,
# SQLAlchemy e psycopg2 saíram daqui junto. Transcrição, pré-visualização Word (pywin32)
# e logo no .xlsx (Pillow) vivem no docservice/, que tem requirements próprio.
python-docx>=1.1
openpyxl>=3.1
PyYAML>=6.0
anthropic>=0.40 # modo IA: reconferência de tempo verbal + ortografia (opcional, só com chave)
